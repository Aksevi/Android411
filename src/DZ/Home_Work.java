package DZ;

public class Home_Work {
    public static void main(String[] args) {
//        System.out.println("Переменная типа byte занимает " + Byte.BYTES + " байт памяти (" + Byte.SIZE + " бит памяти)," + " и принимает значение  в диапазоне от " + Byte.MIN_VALUE + " до " + Byte.MAX_VALUE);
//        System.out.println("Переменная типа short занимает " + Short.BYTES + " байта памяти (" + Short.SIZE + " бит памяти)," + " и принимает значение  в диапазоне от " + Short.MIN_VALUE + " до " + Short.MAX_VALUE);
//        System.out.println("Переменная типа int занимает " + Integer.BYTES + " байта памяти (" + Integer.SIZE + " бита памяти)," + " и принимает значение  в диапазоне от " + Integer.MIN_VALUE + " до " + Integer.MAX_VALUE);
//        System.out.println("Переменная типа long занимает " + Long.BYTES + " байт памяти (" + Long.SIZE + " бита памяти)," + " и принимает значение  в диапазоне от " + Long.MIN_VALUE + " до " + Long.MAX_VALUE);
//        System.out.println("Переменная типа float занимает " + Float.BYTES + " байта памяти (" + Float.SIZE + " бита памяти)," + " и принимает значение  в диапазоне от " + Float.MIN_VALUE + " до " + Float.MAX_VALUE);
//        System.out.println("Переменная типа double занимает " + Double.BYTES + " байт памяти (" + Double.SIZE + " бита памяти)," + " и принимает значение  в диапазоне от " + Double.MIN_VALUE + " до " + Double.MAX_VALUE);
        System.out.println("переменная типа byte занимает " + Byte.BYTES + " байт памяти (" + Byte.SIZE + " бит памяти)," + " и принимает значение в диапазоне от " + Byte.MIN_VALUE + " до " + Byte.MAX_VALUE);
    }
}
